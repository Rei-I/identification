<% @title = '機種の新規登録' %>
<h1>ホーム > 機種管理 > <%= @title %></h1>

<div id="generic-form">
  <%= form_for [:admin, @product_item] do |f| %>
    <%= render 'form', f: f %>
    <div class="buttons">
      <%= f.submit '登録' %>
      <%= link_to 'キャンセル', :admin_product_items %>
    </div>
  <% end %>
</div>