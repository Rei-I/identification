<% @title = '客先の新規登録' %>
<h1>ホーム > 客先管理 > <%= @title %></h1>

<div id="generic-form">
  <%= form_for [:admin, @customer] do |f| %>
    <%= render 'form', f: f %>
    <div class="buttons">
      <%= f.submit '登録' %>
      <%= link_to 'キャンセル', :admin_customers %>
    </div>
  <% end %>
</div>