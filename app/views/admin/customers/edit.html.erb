<% @title = '客先編集' %>
<h1>ホーム > 客先管理 > <%= @title %></h1>

<div id="generic-form">
  <%= form_for [:admin, @customer] do |f| %>
    <%= render 'form', f: f %>
    <div class="buttons">
      <%= f.submit '更新' %>
      <%= link_to 'キャンセル', :admin_customers %>
    </div>
  <% end %>
</div>