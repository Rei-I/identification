<div class="notes">
  <span class="mark">*</span> 印の付いた項目は入力必須です。
</div>
<div>
  <%= f.label :name, '客先名', class: 'required' %>
  <%= f.text_field :name, required: true %>
</div>
<div>
   <%= f.label :abbreviation, '略称', class: 'required' %>
   <%= f.text_field :abbreviation , required: true %> 
</div>